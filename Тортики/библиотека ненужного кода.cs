
namespace Тортики
{
    internal class библиотека_ненужного_кода
    {
        /*public int Strelki(int p)
        {
            int pi = 1;
            while (pi == 1)
            {
                Texti();
                ConsoleKeyInfo key = Console.ReadKey();
                Console.Clear();
                if (key.Key == ConsoleKey.UpArrow)
                {
                    p = p - 1;
                }
                if (key.Key == ConsoleKey.DownArrow)
                {
                    p = p + 1;
                }
                if (key.Key == ConsoleKey.Enter)
                {
                    pi = 0;
                }
                if (p < 0)
                {
                    p = 0;
                }
                Console.SetCursorPosition(0, p);
                Console.WriteLine("->");
            }
            Console.WriteLine(p);*/
        }
}
